syntax = "proto3";

service MessageService {
    rpc Send (Message) returns (MessageResponse) {
    };
}

message Message {
    string uuid = 1;
    string data = 2;
}

message MessageResponse {
    string uuid = 1;
    bool status = 2;
}
